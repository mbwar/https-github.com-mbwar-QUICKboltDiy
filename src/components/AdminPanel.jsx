import React from 'react';

    const AdminPanel = () => {
      return (
        <div>
          <h1>Admin Panel</h1>
          <p>This is the admin panel where you can manage users, guarantees, and contract clauses.</p>
        </div>
      );
    };

    export default AdminPanel;
